name: Release

on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  release:
    runs-on: windows-2019
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v2
      - name: Add nuget to PATH
        uses: nuget/setup-nuget@v1
      - name: Add msbuild to PATH
        uses: microsoft/setup-msbuild@v1.1
      - name: Install nuget packages
        run: nuget restore TMIAutomation\TMIAutomation.csproj -PackagesDirectory packages
      - name: Build app for release ESAPI-v15.6
        run: msbuild TMIAutomation\TMIAutomation.csproj -t:Rebuild -p:Configuration=Release-15.6
      - name: Build app for release ESAPI-v16.1
        run: msbuild TMIAutomation\TMIAutomation.csproj -t:Rebuild -p:Configuration=Release-16.1
      - name: Set artifact names
        run: |
          echo "ESAPI15_RELEASE_ZIP=ESAPI15_TMIAutomation-${{ github.ref_name }}.zip" >> $GITHUB_ENV
          echo "ESAPI16_RELEASE_ZIP=ESAPI16_TMIAutomation-${{ github.ref_name }}.zip" >> $GITHUB_ENV
      - name: Create artifacts for relase ESAPI-v15.6
        uses: vimtor/action-zip@v1
        with:
          files: TMIAutomation\Library\Release-15.6\
          dest: ${{ env.ESAPI15_RELEASE_ZIP }}
      - name: Check artifact was created successfully ESAPI-v15.6
        uses: actions/upload-artifact@v1
        with:
          name: my-artifact
          path: ${{ github.workspace }}\${{ env.ESAPI15_RELEASE_ZIP }}
      - name: Create artifacts for relase ESAPI-v16.1
        uses: vimtor/action-zip@v1
        with:
          files: TMIAutomation\Library\Release-16.1\
          dest: ${{ env.ESAPI16_RELEASE_ZIP }}
      - name: Check artifact was created successfully ESAPI-v16.1
        uses: actions/upload-artifact@v1
        with:
          name: my-artifact
          path: ${{ github.workspace }}\${{ env.ESAPI16_RELEASE_ZIP }}
      - uses: ncipollo/release-action@v1
        with:
          artifacts: ${{ env.ESAPI15_RELEASE_ZIP }},${{ env.ESAPI16_RELEASE_ZIP }}
          draft: true
          name: Test